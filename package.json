{
  "name": "ai-in-ta-telegram-bot",
  "version": "1.6.0",
  "private": true,
  "description": "AI-in-TA Telegram briefing bot (Cloudflare Worker + GitHub Actions delivery)",
  "scripts": {
    "test": "node --test \"shared/**/*.test.mjs\" \"test/**/*.test.mjs\""
  }
}
